use python3 -m http.server 8000 to create a server in the directory.
Now we can use http://localhost:8000/ to access the server from the web browser
hit control+C in terminal to end the web server from the terminal./ also can use kill PID where PID 
